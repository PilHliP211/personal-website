---
interface Props {
  title: string;
  description: string;
  pubDate: Date;
  slug: string;
  tags?: string[];
}

const { title, description, pubDate, slug, tags = [] } = Astro.props;

const formattedDate = pubDate.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
});
---

<article class="group">
  <a
    href={`/blog/${slug}/`}
    class="block no-underline rounded-lg py-4 px-4 -mx-4 transition-colors duration-150 hover:bg-surface"
  >
    <div class="flex items-start justify-between gap-4">
      <div class="flex-1 min-w-0">
        <h2
          class="text-sm font-semibold mb-1 truncate text-text"
        >
          {title}
        </h2>
        <p class="text-sm leading-relaxed line-clamp-2 text-muted">
          {description}
        </p>
        <div class="flex flex-wrap items-center gap-2 mt-2">
          <time
            class="text-xs text-muted"
            datetime={pubDate.toISOString()}
          >
            {formattedDate}
          </time>
          {tags.length > 0 && (
            <span class="text-muted text-xs">&middot;</span>
          )}
          {tags.slice(0, 3).map((tag) => (
            <span class="text-xs font-mono px-1.5 py-0.5 rounded bg-surface text-muted">
              {tag}
            </span>
          ))}
        </div>
      </div>
      <span
        class="text-sm mt-0.5 shrink-0 opacity-0 group-hover:opacity-100 group-focus-within:opacity-100 transition-opacity text-muted"
        aria-hidden="true"
      >&rarr;</span>
    </div>
  </a>
</article>
